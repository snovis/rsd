---
name: rsd:d
description: Shortcut for /rsd:doc. Save a substantial analysis to .rsd/docs/ verbatim. Requires an explicit title.
argument-hint: "<what to save — short description>"
allowed-tools:
  - Read
  - Write
  - Bash
  - Glob
---

Invoke `/rsd:doc` with the same arguments. See `@${CLAUDE_PLUGIN_ROOT}/commands/doc.md` for the full spec.

Execute the /rsd:doc workflow end-to-end.
