---
name: rsd:pu
description: Shortcut for /rsd:pickup. Read the latest handoff and brief the current session.
argument-hint: ""
allowed-tools:
  - Read
  - Bash
  - Glob
---

Invoke `/rsd:pickup`. See `@$HOME/.claude/plugins/rsd/commands/pickup.md` for the full spec.

Execute the /rsd:pickup workflow end-to-end.
