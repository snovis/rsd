---
name: rsd:wd
description: Shortcut for /rsd:walk-done. Finalize the active walk.
argument-hint: "[optional note]"
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
  - Glob
---

Invoke `/rsd:walk-done` with the same arguments. See `@${CLAUDE_PLUGIN_ROOT}/commands/walk-done.md` for the full spec.

Execute the /rsd:walk-done workflow end-to-end.
